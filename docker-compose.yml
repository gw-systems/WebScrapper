services:
  postgres:
    image: postgres:14-alpine
    container_name: webscrapper-postgres
    environment:
      POSTGRES_USER: webscrapper_user
      POSTGRES_PASSWORD: dev_password_123
      POSTGRES_DB: webscrapper
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
      - ./database/migrations/001_initial_schema.sql:/docker-entrypoint-initdb.d/001_schema.sql
      - ./database/seeds/dev_api_keys.sql:/docker-entrypoint-initdb.d/002_seeds.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U webscrapper_user -d webscrapper" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - webscrapper-network

  # Optional: pgAdmin for database management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: webscrapper-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@webscrapper.local
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    volumes:
      - ./pgadmin-data:/var/lib/pgadmin
    depends_on:
      - postgres
    networks:
      - webscrapper-network

networks:
  webscrapper-network:
    driver: bridge

volumes:
  postgres-data:
  pgadmin-data:
